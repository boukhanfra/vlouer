{% extends "GsmLotIndexBundle:Index:base.html.twig" %}
{% trans_default_domain 'FOSUserBundle' %}

{% block contenu %}
<div class="container" style="padding:10px">
         <div class="row">
            <div class="col-xs-6">
 {% image '@GsmLotIndexBundle/Resources/public/images/myspace.jpg' %}
    <img src="{{ asset_url }}" alt="gsmlot phones exchange" class="img-responsive" />
{% endimage %} 
            </div>
            
            <div class="col-xs-5" style="text-align: justify">

    <p>{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</p>
    {% if targetUrl %}
    <p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>
    {% endif %}

			</div>
        </div>
    </div>
   
   {% endblock contenu %}